hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=hash_mode = lf_canonical_text_v1
Every sha256 below is computed over the file's LF-canonicalised text:
    sha256( file_bytes.replace(b'
', b'
') )
A plain `sha256sum` will NOT match on a CRLF checkout. Verify with:
    python backend/research/verify_prereg_hashes.py
=

PREREG_CONDITIONAL_STOPPING_V1.md
sha256 = 5fcae7b6aa069141d0c44f6c54bfac1d87a4e81e2fdba35b7f76f362dfa1a35c
frozen = 2026-07-25
bytes  = 7989

Verify before scoring:
  python -c "import hashlib;print(hashlib.sha256(open('docs/active/PREREG_CONDITIONAL_STOPPING_V1.md','rb').read()).hexdigest())"
A different hash at scoring time means the protocol was edited after freezing,
which invalidates the experiment.

======================================================================
PREREG_BINANCE_VOLATILITY_MOMENTUM_V1.md
sha256 = 0973744b73651e8287b44309c976530f72a3964ceb082703c6b49400564c72f7
frozen = 2026-07-26
bytes  = 11219

Verify before scoring M0:
  python -c "import hashlib;print(hashlib.sha256(open('docs/active/PREREG_BINANCE_VOLATILITY_MOMENTUM_V1.md','rb').read()).hexdigest())"

======================================================================
PREREG_BINANCE_V1_CLARIFICATION_001.md
sha256 = 12bf5e1e5829d320b4d6bbe9a7c3b94af23b33e433b7bdc6782bcb8f7a7db7d6
frozen = 2026-07-26
bytes  = 2862

Completes a parameter the preregistration named but left unvalued.
The preregistration itself is UNCHANGED - verify both:
  python -c "import hashlib;print(hashlib.sha256(open('docs/active/PREREG_BINANCE_V1_CLARIFICATION_001.md','rb').read()).hexdigest())"

======================================================================
PREREG_BINANCE_V1_CLARIFICATION_002.md
sha256 = 320631b2a83aaaca5b21e888d5fcfdf51e416bb1f4429c1bdb988207e3700d3f
frozen = 2026-07-26
bytes  = 3056

Completes a parameter the preregistration named but left unvalued.
The preregistration itself is UNCHANGED - verify both:
  python -c "import hashlib;print(hashlib.sha256(open('docs/active/PREREG_BINANCE_V1_CLARIFICATION_002.md','rb').read()).hexdigest())"

======================================================================
PREREG_BINANCE_V1_CLARIFICATION_003.md
sha256 = 05e3ab773b80e81bb833d38f0e728d8ca9609009ee2c78be890132bcd512f5e7
frozen = 2026-07-26
bytes  = 2800

Completes the required-stream denominator and stale-stream interpretation before collection.
The preregistration itself is UNCHANGED - verify both:
  python -c "import hashlib;print(hashlib.sha256(open('docs/active/PREREG_BINANCE_V1_CLARIFICATION_003.md','rb').read()).hexdigest())"

======================================================================
PREREG_COMPLETE_TRADE_M0_V2.md
sha256 = 138616d3893c5034bddd29be562f73c452e16f570af67ffcb1adda209df793a5
frozen = 2026-07-26
bytes  = 7682

Supersedes the V1 complete-trade gate. The V1 gate was never executed, but its selection unit,
target, label semantics, settlement filter, capacity rule and logging contract have all changed,
and the 395-round July pilot was used to DESIGN those changes - so that dataset is development
data with kill-only authority. The promotion test uses forward evidence collected after this
freeze. Verify:
  python -c "import hashlib;print(hashlib.sha256(open('docs/active/PREREG_COMPLETE_TRADE_M0_V2.md','rb').read()).hexdigest())"
